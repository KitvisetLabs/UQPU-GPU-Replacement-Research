# Eight-Lane Batch 003 — First Verified Win Pipeline

## Core advance
Batch 003 turns the First Verified Win milestone into a machine-readable pipeline:

```text
Workload Contract
 -> Candidate Quantum Paradigm
 -> Provider Fit
 -> State/I/O Accounting
 -> Backend Run Plan
 -> Evidence Record
 -> Verified-Win Gate
 -> 100x Gate
```

## Why this matters
The project can no longer count a promising algorithm, simulator result or cheap internal kernel as a mission win. The pipeline requires a workload contract, state/data accounting, provider target and evidence classification.

## Initial workload order
1. combinatorial optimization;
2. Monte-Carlo-like estimation;
3. structured linear algebra/observable extraction;
4. stateful checkpoint workflow.

## Immediate next experiment
Build one small combinatorial-optimization benchmark with:
- exact or strong classical baseline;
- QUBO/Ising formulation;
- simulator result;
- D-Wave/Braket/IonQ/IBM compatibility assessment;
- state/input/output byte accounting;
- provider pricing estimate;
- real-QPU run only after credentials and explicit cost approval.

## Evidence boundary
Until a REAL_QPU result passes output quality and end-to-end economics, the project remains NOT_YET_DEMONSTRATED for a verified workload advantage.
