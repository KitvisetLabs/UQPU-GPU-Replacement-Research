# Lane D/G — Process-Window Contract

For every critical fabrication step, Lane D specifies an acceptable device-output window while Lane G records candidate tool/process capability and the metrology feedback loop.

A manufacturing route is not accepted merely because a tool nominally performs the process. The controlled process window must credibly overlap the required device window.

**Evidence status:** ENGINEERING_FRAMEWORK / NUMERIC_CALIBRATION_PENDING.
